import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: 10,
        borderRadius: 16,
        backgroundColor: '#DFDFDF',
        bottom: 40,
    },
})
